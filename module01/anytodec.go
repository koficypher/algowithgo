package module01

import (
	"fmt"
)

// AnyToDec converts a binary digit to a base by
func AnyToDec(value string, base int) int {
	//	var result int
	for _, num := range value {
		//multiplier := base * *int(value[i])
		//result = int(num) *
		fmt.Printf("%d\n", num)

	}
	return 0
}
